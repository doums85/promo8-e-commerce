import { Layout } from '../../components';

export default function Detail() {
  return (
    <Layout>
      <section className="section">
        <h1 className="section-title">Detail Page</h1>
      </section>
    </Layout>
  );
}
